@using WebApp.AppCode;

@{
    ViewBag.Title = "home";    
}

<h2>Welcome Home !!! @Model.FirstName</h2>
<ui>
@foreach (var item in @Model.Models)
{ 
        <li>
        @item.Make -- @ECMSViewHelper.Eval(item.Year.ToString())
    </li>
}   
    </ui>
@Html.Partial("/Views/1/Partial1.cshtml");
